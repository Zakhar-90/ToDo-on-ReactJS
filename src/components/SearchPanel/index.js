import './searchPanel.css';

const SearchPanel = () => {

    return (
        <input
            type="text"
            placeholder='Search'
            className="form-control search-input"
        />
    );
};

export default SearchPanel;
